rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Siperditan persuratan system - real-time collaborative government records
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
